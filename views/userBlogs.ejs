<!DOCTYPE html>
<html lang="en">
<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Blogs</title>
</head>
<body>
    <%- include("./partials/nav") %>
    <div class="container mt-3">
        <h3>Blogs by <%= userBlogs.username %></h3><hr>

        <% if (locals.userBlogs.BlogsWritten.length === 0) { %>
         <h4>no blogs to show Currently </h4>

        <% } else { %>
        <div class="row g-4">
            <% userBlogs.BlogsWritten.forEach(userBlog => { %>                              <!--mistake was userBlogs is an object, not an array. The .forEach function works with arrays, so you'll want to iterate over the BlogsWritten array instead.-->
                <div class="col-12 col-sm-6 col-md-4 col-lg-3 d-flex align-items-stretch">
                    <div class="card h-100 w-100">
                        <img class="card-img-top" src="<%= userBlog.coverImage %>" alt="Card image cap">
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title"><%= userBlog.title %></h5>
                            <a href="/blog/<%= userBlog._id %>" class="btn btn-primary mt-auto">Read</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
        <% } %>
    </div>

    <%- include("./partials/script") %>
</body>
</html>
