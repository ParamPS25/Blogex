<!DOCTYPE html>
<html lang="en">
    <head>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">

        <style>
            .blog-container {
                border: 1px solid #787575;
                border-radius: 10px;
                background-color: #f9f9f9;
                overflow: hidden;
            }
            .blog-image {
                max-height: 500px;
                max-width: 100%;
                border-radius: 10px;
            }
            .blog-content {
                white-space: pre-wrap;
                font-family: 'Georgia', 'Times New Roman', Times, serif;
                font-size: 1.1em;
                line-height: 1.6;
                overflow-wrap: break-word;
            }
            .blog-author {
                display: flex;
                align-items: center;
            }
            .author-image {
                width: 50px;
                height: 50px;
                border-radius: 50%;
            }
        </style>
        
        </head>
<title>Blog Post</title>
<body>
    <%- include("./partials/nav") %>
    <div class="container mt-4 p-4 blog-container">
        <h2 class="text-center"><%= fullBlog.title %></h2>
        <hr>
        <div class="text-center">
            <img src="<%= fullBlog.coverImage %>" class="img-fluid blog-image">
        </div>
        <div class="mt-4 blog-content">
            <%= fullBlog.bodyContent %>
        </div>
        <div class="blog-author mt-4 d-flex align-items-center">
            <img src="<%= currentUser.profileImg %>" class="author-image">
            <div class="ms-3">
                <h4>Written By <%= currentUser.username %></h4>
                <p><%= fullBlog.createdAt %></p>
            </div>
        </div>
    </div>
    <%- include("./partials/script") %>
</body>
</html>
